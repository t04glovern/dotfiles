# nvm
#export NVM_DIR="$HOME/.nvm"
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
#[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# Jenv (relies on the jenv brew install)
#export PATH="$HOME/.jenv/bin:$PATH"
#eval "$(jenv init -)"

# rbenv
#export PATH="$HOME/.rbenv/bin:$PATH"
#eval "$(rbenv init -)"

# Android (relies on android studio being installed with platform tools)
#export PATH="$HOME/Library/Android/sdk/platform-tools:$PATH"
#export ANDROID_HOME="$HOME/Library/Android/sdk"

# iTerm shell integrations (relies on shell integration script being run from iTerm2 menu)
#test -e "${HOME}/.iterm2_shell_integration.bash" && source "${HOME}/.iterm2_shell_integration.bash"

# Flutter
#export PATH="$HOME/Projects/misc/flutter/bin:$PATH"
#export FLUTTER_ROOT="$HOME/Projects/misc/flutter"
